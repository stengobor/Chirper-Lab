import React from 'react';

const Username = (props) => {
    return (

        <>
            <header> @{props.currentUsername}</header>
        </>

    
        
    );
};


export default Username;